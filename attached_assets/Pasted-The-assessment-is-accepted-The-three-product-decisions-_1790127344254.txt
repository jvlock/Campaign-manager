The assessment is accepted. The three product decisions are now resolved:

1. Advisory failures do not prevent completion; they remain visible.
2. Material-change notices use a tiered model with a one-hour operational SLA.
3. The attended-follow-up clock starts at recorded event completion; delayed attendance data does not reset it.

The next step should implement the 23-rule Setup batch and the reusable evidence foundation it requires.

Give Replit this prompt:

⸻

Phase 2A-5B — Evidence Foundation and Setup Evaluator Batch

Implement the reusable in-memory evidence model and the 23 evaluators assigned to Batch 1 — Setup in the approved evaluator coverage plan.

Do not implement the other 68 missing evaluators in this phase.

1. Starting state

Expected branch:

feature/webinar-standard-engine

Expected HEAD:

08479495730af0debcf58ebf0f85cbb64d213a86

Confirm:

* branch, HEAD, parent, and working-tree state;
* 106 canonical rule IDs;
* 15 implemented evaluators;
* 91 missing evaluators;
* the Setup batch contains exactly 23 missing rules;
* baseline remains 32 test files and 560 passing tests;
* non-incremental type check passes;
* @js-temporal/polyfill is exactly 0.5.1;
* all five canonical hashes match.

The Phase 2A-5A assessment upload is expected to be untracked. Read it, hash it, and confirm it contains only the previously delivered assessment prompt. If confirmed, delete it without committing it.

Stop for any other substantive repository difference.

2. Authoritative sources

Use:

* docs/standards/webinar/WEB-STANDARD-001.rules.json
* docs/standards/webinar/WEB-STANDARD-001.md
* docs/standards/webinar/WEBINAR-PILOT-IMPLEMENTATION-AUTHORIZATION.md
* docs/verification/phase-2a-5-evaluator-coverage-plan.md
* existing catalog, evaluator, exception, readiness, scheduling, audience-state, and time modules.

The coverage-plan document controls batch membership but does not override canonical rule content.

Do not use .agents/memory/, pasted prompts, or workbooks as requirements sources.

3. Record the resolved product decisions

Update the decision register in:

docs/verification/phase-2a-5-evaluator-coverage-plan.md

Mark these decisions resolved:

WEB-DONE-001

Failed Warning, Recommended-default, or Optional rules do not prevent Complete. They remain visible in the completion result.

Complete is prevented by:

* unresolved applicable blockers;
* missing required obligations;
* missing required blocker evidence;
* missing evaluator coverage;
* unresolved completion dependencies.

WEB-REG-005 and WEB-REG-006

Use a tiered material-change model:

Always material attendance-impacting changes

* cancellation;
* event date;
* start or end time;
* time zone;
* delivery format;
* venue or location;
* event platform;
* join link;
* access instructions.

These create a participant-notice obligation immediately when the change is recorded. The operational due time is within one elapsed hour. If the event begins sooner, the due time is no later than event start. A late obligation becomes overdue; it must not create a backdated send.

Material only after being published or communicated

* public session title or topic;
* advertised speaker roster;
* published agenda;
* participant-facing access requirements.

Internal owner, internal notes, measurement configuration, and other non-participant-facing administrative changes do not create participant-notice obligations.

This decision creates planning obligations only and does not authorize sending.

WEB-FU-ATT-001

The one-business-day attended-follow-up clock begins at the recorded actual event-completion instant.

* Delayed attendance data does not reset or extend the deadline.
* If attendance data arrives late, the follow-up obligation may already be overdue.
* Missing event-completion time is unavailable required evidence, not an automatic pass.
* Use the existing Monday–Friday, event-time-zone business-day convention without holidays.

Do not implement the registrant or completion rules in this batch unless they are among the plan’s exact 23 Setup rules. Record these decisions now for their later batches.

4. Reusable manual-evidence model

Create a pure, immutable, in-memory evidence model containing at least:

* immutable evidence ID;
* canonical rule ID;
* standard ID;
* exact standard version;
* evidence status;
* evidence description;
* supplied-by name or pilot identifier;
* explicit identityVerified: false pilot marker;
* supplied timestamp;
* evidence source or attachment reference where applicable;
* effective date;
* expiration or validity date where applicable;
* scope identifier, such as session, participant, communication, or deliverable;
* notes.

Validation must reject:

* unknown rule ID;
* wrong standard or version;
* missing evidence ID;
* duplicate evidence ID;
* mismatched scope;
* missing required timestamp;
* future supplied timestamp when not logically permitted;
* expired evidence;
* malformed source reference;
* any claim that the pilot supplier identity is verified.

Do not add authentication, persistence, upload capability, database tables, routes, or UI.

5. Evidence is not an exception

Preserve this distinction:

* Evidence determines whether a rule can be evaluated and whether it passes or fails.
* An exception may resolve an eligible blocker that still failed.
* Evidence must never silently resolve a failure.
* An exception must never substitute for missing evidence.
* A rule’s original evaluation result remains visible after exception resolution.

Add tests proving this separation.

6. Evaluation-result semantics

Inspect the current result types and implement only the minimum additive change required to represent unavailable evidence.

Required distinctions:

* pass;
* fail;
* not applicable;
* evidence unavailable;
* invalid context;
* evaluator unavailable through registry-coverage reporting;
* failed but resolved by exception.

Do not rename or remove existing statuses unnecessarily.

Readiness handling:

* unavailable evidence for an applicable Mandatory or Conditional blocker makes the stage incomplete, unless another unresolved failure already makes it blocked;
* unavailable evidence for a Warning, Recommended-default, or Optional rule remains visible as an unassessed advisory and does not independently block or make the stage incomplete;
* evaluator unavailable remains missing coverage and therefore incomplete;
* not-applicable rules do not block;
* a failed advisory remains visible but nonblocking;
* a valid exception cannot resolve evidence-unavailable status because no failure has yet been established.

Update readiness tests where required without weakening the corrected blocking classifier.

7. Implement the 23 Setup evaluators

Load the exact 23 Batch 1 rule IDs from the committed coverage plan. Do not reconstruct membership from naming conventions.

For every Batch 1 rule:

* create an explicit evaluator;
* register it under the exact canonical rule ID;
* consume the context or evidence identified in the coverage plan;
* preserve the canonical trigger;
* return pass, fail, not applicable, evidence unavailable, or invalid context as appropriate;
* preserve rule-specific failure message and resolution guidance;
* preserve exception eligibility;
* add rule-specific tests.

Shared named predicates are allowed, but each rule must retain:

* its own evaluator or explicit adapter;
* its own registry entry;
* its own canonical output;
* traceability to its canonical rule.

Do not create a generic fallback evaluator.

Do not treat field presence as proof of quality where the rule requires testing, confirmation, approval, or review.

8. Context changes

Add only the typed context fields identified for Batch 1 in the coverage plan.

Requirements:

* readonly and immutable;
* no session-level attendance field;
* no database model;
* no runtime environment access;
* no automatic clock access;
* no invented taxonomy values;
* no governed-source bypass.

If a Batch 1 evaluator unexpectedly requires a product decision not recorded in the coverage plan, stop and report that rule instead of guessing.

9. Expected coverage

If all 23 Batch 1 evaluators are implemented and registered successfully:

* existing coverage: 15;
* new Setup evaluators: 23;
* expected coverage: 38 of 106;
* expected remaining: 68.

Mechanically reconcile:

* no duplicate evaluator IDs;
* no unknown IDs;
* no missing Batch 1 IDs;
* no evaluator added outside Batch 1;
* registry count equals 38;
* missing count equals 68.

Do not force these numbers if a documented stop condition prevents a rule from being implemented. Report the exact difference.

10. Required tests

Add independently named tests covering:

Evidence model

* valid evidence;
* unknown rule;
* wrong standard;
* wrong version;
* duplicate evidence ID;
* scope mismatch;
* missing timestamp;
* future timestamp;
* expiration;
* malformed source;
* verified-identity claim rejected;
* input and result immutability.

Evidence and exceptions

* evidence supports evaluation;
* evidence does not resolve failure;
* exception does not replace missing evidence;
* valid exception retains the failed result;
* expired or mismatched evidence remains unavailable.

Result semantics

* blocker evidence unavailable → incomplete;
* advisory evidence unavailable → visible and nonblocking;
* evaluator unavailable → missing coverage and incomplete;
* failed advisory → visible and nonblocking;
* applicable blocker failure → blocked;
* not applicable → nonblocking;
* blocker plus unavailable evidence elsewhere → blocked with incompleteness visible.

Setup evaluators

For each of the 23 rules, independently test as applicable:

* pass;
* fail;
* not applicable;
* evidence unavailable;
* invalid context;
* exception behavior where eligible;
* canonical failure and resolution output.

Coverage

* exactly 38 registered evaluator IDs;
* exactly 68 missing;
* all 23 Batch 1 IDs registered;
* no rules from later batches registered;
* deterministic registry and evaluation output.

11. Scope restrictions

Do not implement:

* Batch 2 through Batch 6 evaluators;
* governed taxonomy, naming, campaign-code, or UTM integration;
* scheduling changes;
* audience-state changes;
* database persistence;
* migrations;
* routes or API integration;
* UI;
* authentication;
* external sending;
* publishing;
* deployment;
* workbook ingestion.

Do not modify canonical webinar documents.

Do not add product policy under .agents/memory/.

12. Verification gate

Run fresh:

1. evidence-model tests;
2. evidence/exception separation tests;
3. Setup evaluator tests;
4. coverage and registry tests;
5. readiness tests;
6. exception tests;
7. existing evaluator tests;
8. scheduling tests;
9. audience-state tests;
10. business-day/time tests;
11. structural and containment tests;
12. catalog-integrity tests;
13. rule-ID drift check;
14. non-incremental type check;
15. complete API suite against a new disposable database;
16. frontend suite;
17. all five canonical hash checks.

Report:

* test files;
* top-level tests;
* subtests;
* individual tests;
* failures;
* skipped, cancelled, and todo tests;
* setup and teardown failures;
* type errors;
* all 21 migrations;
* database shutdown and cleanup;
* actual evaluator coverage.

Do not force a predetermined total-test count.

Use explicit file staging. Do not commit uploaded prompts or unrelated files.

Suggested commit title:

Add webinar setup evaluators and evidence model

13. Completion report

Report:

* starting and ending commits;
* every changed file;
* deleted untracked prompt hash and disposition;
* decision-register updates;
* evidence model;
* result-type changes;
* exact 23 implemented rule IDs;
* shared predicates used;
* context fields added;
* actual coverage and missing counts;
* all verification results;
* canonical hashes;
* confirmation that no later batch, database, API, UI, sending, publishing, or deployment work began;
* any stopped or unresolved rule.

Stop after reporting.

⸻